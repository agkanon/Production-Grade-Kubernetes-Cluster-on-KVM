# IMPORTANT: Replace stringData values with your actual credentials before applying.
# In production use a secrets manager (Vault, Sealed Secrets, External Secrets Operator).
apiVersion: v1
kind: Secret
metadata:
  name: bmi-secrets
  namespace: production
type: Opaque
stringData:
  db-user: "bmi_user"
  db-password: "StrongP@ssw0rd!"
  # Full connection string consumed directly by the Node.js backend (process.env.DATABASE_URL)
  database-url: "postgresql://bmi_user:StrongP@ssw0rd!@postgres-service.production.svc.cluster.local:5432/bmidb"
